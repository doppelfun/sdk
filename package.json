{
  "name": "doppel-sdk",
  "version": "0.2.0",
  "private": true,
  "license": "MIT",
  "description": "Doppel SDK monorepo: core client and agent claw",
  "scripts": {
    "build": "pnpm -r run build",
    "clean": "pnpm -r run clean",
    "pack:check": "pnpm run build && pnpm publish -r --dry-run --no-git-checks"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.14.2"
}
